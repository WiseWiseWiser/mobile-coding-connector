# Scenario

**Feature**: remote-agent agent-run watch (P4 short stream)

```
watch <session-id> -> short inject stream | error
```

## Preconditions

- L2 Watch inject returns promptly (no long hang).
